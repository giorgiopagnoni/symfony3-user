{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>{{ 'user.profile'|trans }}</h1>
                {{ form_start(form) }}

                {{ form_row(form.profile) }}

                <button type="submit" class="btn btn-primary" formnovalidate>Continue</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}